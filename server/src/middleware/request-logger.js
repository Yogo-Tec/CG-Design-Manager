import{randomUUID}from"node:crypto";export function requestLogger(req,res,next){const started=performance.now(),requestId=req.get("x-request-id")||randomUUID();req.requestId=requestId;res.setHeader("x-request-id",requestId);res.on("finish",()=>{const entry={timestamp:new Date().toISOString(),level:res.statusCode>=500?"error":res.statusCode>=400?"warn":"info",requestId,method:req.method,route:req.originalUrl.split("?")[0],status:res.statusCode,durationMs:Number((performance.now()-started).toFixed(1)),userId:req.user?.sub||null};console.log(JSON.stringify(entry))});next()}
