import { Router } from "express";import multer from "multer";import path from "node:path";import { randomUUID } from "node:crypto";import { allowRoles } from "../middleware/auth.middleware.js";
import{databaseEnabled,query}from"../config/database.js";
const allowed=new Set(["image/jpeg","image/png","image/webp","image/svg+xml","application/pdf","application/zip","application/postscript"]);const storage=multer.diskStorage({destination:(_r,_f,cb)=>cb(null,process.env.STORAGE_PATH||"uploads"),filename:(_r,file,cb)=>cb(null,`${Date.now()}-${randomUUID()}${path.extname(file.originalname).toLowerCase().slice(0,10)}`)});const upload=multer({storage,limits:{fileSize:Number(process.env.UPLOAD_MAX_SIZE_MB||50)*1024*1024,files:5},fileFilter:(_r,file,cb)=>cb(allowed.has(file.mimetype)?null:new Error("Unsupported file type"),allowed.has(file.mimetype))});const router=Router(),files=[];
router.get("/",async(req,res,next)=>{try{if(databaseEnabled){const values=[],scope=req.user.role==="ADMIN"?"":`WHERE uploaded_by=$1`;if(scope)values.push(req.user.sub);const rows=(await query(`SELECT id,original_name name,stored_name "storedName",mime_type "mimeType",file_size size,uploaded_by "ownerId",design_job_id "jobId",version_no version,created_at "createdAt" FROM design_files ${scope} ORDER BY created_at DESC`,values)).rows;return res.json({data:{files:rows}})}res.json({data:{files:files.filter(x=>req.user.role==="ADMIN"||x.ownerId===req.user.sub)}})}catch(e){next(e)}});router.post("/",allowRoles("ADMIN","DESIGNER"),upload.array("files",5),async(req,res,next)=>{try{const saved=[];for(const file of req.files){if(databaseEnabled){const row=(await query(`INSERT INTO design_files(design_job_id,uploaded_by,original_name,stored_name,mime_type,file_size,version_no) VALUES($1,$2,$3,$4,$5,$6,$7) RETURNING id,original_name name,stored_name "storedName",mime_type "mimeType",file_size size,uploaded_by "ownerId",design_job_id "jobId",version_no version,created_at "createdAt"`,[req.body.jobId||null,req.user.sub,file.originalname,file.filename,file.mimetype,file.size,Number(req.body.version||1)])).rows[0];saved.push(row)}else saved.push({id:randomUUID(),name:file.originalname,storedName:file.filename,mimeType:file.mimetype,size:file.size,ownerId:req.user.sub,jobId:req.body.jobId||null,version:req.body.version||"1",createdAt:new Date().toISOString()})}if(!databaseEnabled)files.unshift(...saved);res.status(201).json({data:{files:saved}})}catch(e){next(e)}});export default router;
