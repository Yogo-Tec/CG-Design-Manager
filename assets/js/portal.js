const q=s=>document.querySelector(s),esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
const token=new URLSearchParams(location.search).get("token")||"demo-client-token";
try{const response=await fetch(`/api/portal/${encodeURIComponent(token)}`),payload=await response.json();if(!response.ok)throw new Error(payload.message);const data=payload.data;q("#content").innerHTML=`<header class="review-header"><div><p class="eyebrow mb-0">Client approval · Version ${esc(data.proof.version)}</p><h2>${esc(data.project.name)}</h2><p>${esc(data.project.client)} · Deadline ${data.project.deadline?new Date(data.project.deadline).toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"}):"Not set"}</p></div><span class="status-pill">${esc(String(data.proof.status).replaceAll("_"," "))}</span></header><div class="review-grid"><section class="proof-stage"><div class="proof-canvas"><div class="proof-mark"><span>CG</span><strong>${esc(data.proof.title||data.project.name)}</strong><small>Secure proof preview · Version ${esc(data.proof.version)}</small></div></div></section><aside class="decision-panel"><p class="eyebrow">Your decision</p><h3>Ready to respond?</h3><p>Approve this version to move the project forward, or request changes with clear feedback.</p><div class="review-meta"><div><span>Project status</span><strong>${esc(String(data.project.status).replaceAll("_"," "))}</strong></div><div><span>Proof version</span><strong>V${esc(data.proof.version)}</strong></div></div><form id="decision"><div id="decisionError" class="alert alert-danger d-none" role="alert"></div><label class="form-label" for="feedback">Feedback <span class="text-secondary">(optional)</span></label><textarea class="form-control" id="feedback" name="feedback" rows="5" maxlength="2000" placeholder="Describe any changes clearly for the design team"></textarea><div class="decision-actions"><button class="btn btn-success" name="decision" value="APPROVED"><i class="bi bi-check2-circle"></i> Approve this proof</button><button class="btn btn-outline-danger" name="decision" value="CHANGES_REQUESTED"><i class="bi bi-arrow-repeat"></i> Request changes</button></div></form></aside></div>`;q("#decision").addEventListener("submit",async event=>{event.preventDefault();const button=event.submitter,feedback=new FormData(event.currentTarget).get("feedback");button.disabled=true;button.innerHTML='<span class="spinner-border spinner-border-sm"></span> Recording decision';try{const result=await fetch(`/api/portal/${encodeURIComponent(token)}/decision`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({decision:button.value,feedback})});if(!result.ok)throw new Error((await result.json()).message);q("#content").innerHTML='<div class="success-state"><i class="bi bi-check-circle-fill"></i><h2>Decision recorded</h2><p>Thank you. The design team has received your response.</p></div>'}catch(error){button.disabled=false;button.textContent="Try again";q("#decisionError").textContent=error.message;q("#decisionError").classList.remove("d-none")}})}catch(error){q("#content").innerHTML=`<div class="error-state"><i class="bi bi-exclamation-circle"></i><h2>Approval link unavailable</h2><p>${esc(error.message)}</p></div>`}
